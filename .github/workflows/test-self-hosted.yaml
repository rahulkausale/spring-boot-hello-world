name: Testing self-hosted runner


on:
  workflow_dispatch:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted
    steps:
    - name: Know the env
      run: |
        pwd
        ls
        env
    - name: Hello world
      run: echo "Hello World"
